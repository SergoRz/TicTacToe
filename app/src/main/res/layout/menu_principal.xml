<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/activity_main"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    tools:context="com.example.versus.tictactoe.MainActivity"
    android:background="@mipmap/fondo_inicio"
    tools:background="@mipmap/fondo_inicio">

    <ImageView
        android:contentDescription="@string/desc"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        app:srcCompat="@mipmap/tresenrayatitulo"
        android:id="@+id/ivTexto"
        android:layout_below="@+id/ivFoto"
        android:layout_centerHorizontal="true"
        android:layout_marginTop="18dp" />

    <ImageView
        android:contentDescription="@string/desc"
        android:layout_width="300dp"
        android:layout_height="300dp"
        app:srcCompat="@mipmap/tic_tac_toe"
        android:id="@+id/ivFoto"
        android:layout_alignParentTop="true"
        android:layout_centerHorizontal="true" />

    <Button
        android:text="@string/play"
        android:textSize="35sp"
        android:layout_width="150dp"
        android:layout_height="750dp"
        android:id="@+id/btnPlay"
        android:layout_below="@+id/ivTexto"
        android:layout_centerHorizontal="true"
        android:layout_marginTop="16dp" />
</RelativeLayout>
